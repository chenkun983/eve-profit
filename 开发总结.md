# EVE国服工业订单系统 — 开发总结

> 日期：2026-05-29 ~ 2026-06-07
> 项目地址：`D:\agent_workspace\EVE国服服务器项目`
> 服务器：`xiaonaizhao@8.152.194.0` → `/home/xiaonaizhao/eve-profit`

---

## 一、订单系统（完整实现）

### 采购单流程
- **发单** → 订单池公开 → **制造商接单**（需制造商身份）→ 状态「制作中」
- **制造商交付** → 状态「待验收」→ **下单人确认收货** → 状态「已完成」
- 下单人 5 天未确认 → **自动确认**

### 出售单流程
- **发单** → 订单池公开 → **所有人可接**（不限制身份）→ 状态「待交货」
- **卖家（下单人）交货** → 状态「待确认」→ **买家确认收货** → 状态「已完成」
- 卖家交货后 3 天买家未确认 → **自动确认**

### 订单池
- 显示所有公开订单（采购/出售），5 张卡片一行
- **搜索框**：按物品名关键字实时过滤，防抖 300ms
- **类型筛选**：全部 / 采购单 / 出售单
- **分页**：每页 50 张，底部页码导航

### 我的订单（我发布的）
- **状态筛选**：全部 / 公开 / 已完成 / 已取消
- **类型筛选**：全部类型 / 采购单 / 出售单
- 每张订单展示接单明细：物品名、接单人、数量、状态、操作按钮
  - 采购单待验收 → 下单人可「确认收货」
  - 出售单待交货 → 卖家可「交货」
- 无活跃接单的公开订单可「删除订单」

### 我的接单（我接受的）
- **状态筛选**：全部 / 进行中 / 已完成 / 已取消
- **类型标签**：采购单（绿色） / 出售单（紫色）
- 采购单：制造商可「取消」「去交付」
- 出售单：买家可「取消接单」，卖家已发货时显示「✅ 卖家已发货」+「确认收货」

### 工业管理（制造商专用）
- **采购单卡片**：我接的采购单，带【采购】绿色标签
- **出售单卡片**：我发布的出售单被接后，带【出售】紫色标签
- 点击卡片弹出详情弹窗：
  - 采购单：取消接单 / 确认接单（进入交付） / 标记已交付
  - 出售单：取消接单 / 交货
- 卡片实时刷新（取消/确认/交付后即时更新）

### 权限控制
- 采购单仅制造商可接（后端额外校验）
- 出售单所有人可接
- 订单系统标签对所有用户可见，游客点击弹出登录

### 角色反转（后端核心逻辑）
- `update_acceptance_status` 区分采购单/出售单的权限：
  - **采购单**：接单方（制造商）可交付，下单人可确认完成
  - **出售单**：下单人（卖家）可交货，接单方（买家）可确认完成

---

## 二、新页面

### 📖 使用说明
- 三种角色功能对照表（游客→普通用户→制造商）
- 升级制造商流程（含 baby oye 收款人截图 + 申请示意图）
- 订单系统操作说明

### ☕ 投喂
- 支付宝收款码
- 联系方式：QQ 58309362 / 邮箱 58309362@qq.com
- 游戏内联系 baby oye 反馈 BUG
- 未来计划说明

---

## 三、体验优化

- **暗色滚动条**：全局 `::-webkit-scrollbar` 细条深灰色
- **搜索下拉菜单**：超出视口底部时自动显示在输入框上方
- **联系人显示**：所有位置优先显示游戏内 ID（contact_name），其次网站用户名
- **浏览器密码弹窗**：`autocomplete="off"` + 初始 `readonly` 避开
- **输入框宽度**：搜索框 180px，接单数输入框 100px

---

## 四、后端新接口

| 路由 | 说明 |
|------|------|
| `GET /logo` | EVE Logo（圆形展示） |
| `GET /img/upgrade-guide` | 申请制造商示意图 |
| `GET /img/alipay` | 支付宝收款码 |
| `GET /img/baby` | baby oye 角色截图 |

---

## 五、数据库修复记录（2026-06-07）

服务器 `pkill -f main.py` 强制杀进程导致 SQLite WAL 文件异常，`users.db` 损坏。
最终方案：重建 `users.db`（仅建核心表结构，其余由 `auth.py` 的 `init_db()` 自动补全）。
⚠️ 用户数据丢失，需重新注册账号并手动分配角色。

---

## 六、部署命令

```bash
# 推代码
git add .
git commit -m "提交信息"
git push

# 拉代码并重启（服务器端）
cd /home/xiaonaizhao/eve-profit
git pull
pkill -f main.py
nohup venv/bin/python3 main.py > eve.log 2>&1 &
```

---

## 七、待办 / 已知问题

- [ ] 分仓库切换回到列表时订单卡片需重新触发 `loadIndustryOrders`
- [ ] 数据库定期备份机制（防止 WAL 损坏）
- [ ] 考虑域名绑定 + HTTPS

---

*文档由 HanaAgent 自动生成于 2026-06-07*
